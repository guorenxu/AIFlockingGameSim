#ifndef STATECODES_H
#define STATECODES_H

enum TeamCode {NONE, RED, BLUE};

enum BigStates {CHASEALL, STEAL, FLEE, AVOIDSTEALER};

enum TeamStates {OFFENSIVE, DEFENSIVE};

enum Roles {CHASER, STEALER};

enum MessageTypes {TARGETED, UNTARGETED};

enum TeamID {REDTEAM, BLUETEAM};

#endif